import { Prose } from "../components/Prose";

export function meta() {
  return [{ title: "Commission — SprueTube" }];
}

/** Placeholder — replaced by the feature build. */
export default function Commission() {
  return (
    <Prose title="Commission">
      <p>Not built yet.</p>
    </Prose>
  );
}
