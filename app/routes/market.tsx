import { Prose } from "../components/Prose";

export function meta() {
  return [{ title: "Buy and sell — SprueTube" }];
}

/** Placeholder — replaced by the feature build. */
export default function Market() {
  return (
    <Prose title="Buy and sell">
      <p>Not built yet.</p>
    </Prose>
  );
}
