import { Prose } from "../components/Prose";

export function meta() {
  return [{ title: "News — SprueTube" }];
}

/** Placeholder — replaced by the feature build. */
export default function News() {
  return (
    <Prose title="News">
      <p>Not built yet.</p>
    </Prose>
  );
}
