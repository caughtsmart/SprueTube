import { Prose } from "../components/Prose";

export function meta() {
  return [{ title: "Listing — SprueTube" }];
}

/** Placeholder — replaced by the feature build. */
export default function ListingEdit() {
  return (
    <Prose title="Listing">
      <p>Not built yet.</p>
    </Prose>
  );
}
