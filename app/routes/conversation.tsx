import { Prose } from "../components/Prose";

export function meta() {
  return [{ title: "Messages — SprueTube" }];
}

/** Placeholder — replaced by the feature build. */
export default function Conversation() {
  return (
    <Prose title="Messages">
      <p>Not built yet.</p>
    </Prose>
  );
}
